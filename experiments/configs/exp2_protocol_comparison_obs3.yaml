# Experiment 2 (obs3): Protocol Comparison — partial observability
# obs_radius=3: agents must rely on messages → protocol choice matters here
# f=0.25 (2/8 Byzantine), vary protocol across all 5 options (EXP-06)
#
# OBS_DIM = 4 + 49 + 14 = 67  (n_seekers=8, grid_size=16, obs_radius=3)
# Do NOT mix with obs7 checkpoints (OBS_DIM=243) — state dict will mismatch.
#
# At obs_radius=3 agents cover only 19.1% of the map → messages are the
# primary source of hider-position information → protocol choice matters.
# Trimmed-mean and reputation protocols expected to show clearest recovery.
#
# Run with:
#   python scripts/run_sweep.py \
#     --config experiments/configs/exp2_protocol_comparison_obs3.yaml \
#     --checkpoint_tag obs3

experiment:
  name: exp2_protocol_comparison_obs3
  description: "Protocol comparison at f=0.25 (2/8 Byzantine), obs_radius=3"

env:
  grid_size: 16
  n_seekers: 8
  obs_radius: 3
  obstacle_density: 0.25
  max_steps: 500

training:
  algorithm: mappo
  n_episodes: 200
  n_seeds: 3

sweep:
  byzantine_fraction: [0.25]
  protocol: [none, broadcast, gossip, trimmed_mean, reputation]

logging:
  metrics:
    - capture_rate
    - capture_time
    - policy_entropy
    - convergence_episode
  output_dir: experiments/results/exp2_obs3/
  wandb_project: marl-byzantine-pursuit
